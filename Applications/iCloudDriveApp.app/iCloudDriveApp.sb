;;
;; Sandbox permissions
;;

;;
;; We use LaunchServices to open a document in the default app
;;
(allow mach-lookup
  (global-name "com.apple.lsd.mapdb")
  (global-name "com.apple.lsd.modifydb")
)

